import {Piece} from './piece.model';

export class Bishop extends Piece {
    constructor(isWhite: boolean) {
        if (isWhite) {
            super('');
        } else {
            super('');
        }
    }
}