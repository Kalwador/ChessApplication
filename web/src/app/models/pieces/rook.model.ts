import {Piece} from './piece.model';

export class Rook extends Piece {
    constructor(isWhite: boolean) {
        if (isWhite) {
            super('');
        } else {
            super('');
        }
    }
}